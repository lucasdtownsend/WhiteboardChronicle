@model IEnumerable<Whiteboard.Models.TaggedWhiteboard>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tag.TagName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WhiteboardItem.ImageURL)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tag.TagName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WhiteboardItem.ImageURL)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TaggedWhiteboardID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TaggedWhiteboardID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TaggedWhiteboardID })
        </td>
    </tr>
}

</table>
